[pytest]
testpaths = .
python_files = test_*.py
markers =
    smoke: quick testbed checks
    distribution: tests for traffic distribution across paths
    stickiness: tests for per-flow consistency
addopts = -ra -v
